<div class="player_panel">
  <div class="player_info_subpanel">
    <div class="player_photo">
      <%= player_photo(player) %>
    </div>
    <div class="player_info">
      <h3><%= player.name%></h3>
      <ul class="player_stats_list">
        <li>Partidos jugados:<%= player.stat.wins + player.stat.lost %> </li>
        <li>Ganados: <%= player.stat.wins %> </li>
        <li>Perdidos: <%= player.stat.lost %></li>
      </ul>
    </div>
  </div>
  <div class="player_emblems_subpanel">
    Emblemas: <%=player_achievements_panel(player)%>
  </div>
</div>
