<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:og="http://ogp.me/ns#"
      xmlns:fb="http://www.facebook.com/2008/fbml">
<head>
  <title>Padelotron</title>

  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag :jquery_ui %>
  <%= javascript_include_tag "session"%>
  <%= csrf_meta_tag %>
  <%= facebook_app_id %>
  <%=  google_analytics %>
  
</head>
<body>
  

  <% if customer_signed_in? %>
      <h4>Welcome <%= current_customer.name %> <%=current_customer.surname %></h4>
      <%=  link_to("Logout", destroy_customer_session_path, :method => :delete) %>
  <% else %>
       <%= link_to("Log in", new_customer_session_path) %> -
       <%= link_to("Create an account", new_customer_path) %>
    <%  end %>
<%= customer_top_navigation_menu %>

<%= yield %>

</body>
</html>
